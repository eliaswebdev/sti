<%= simple_form_for @dependente do |f| %>
  <%= f.association :pessoa_fisica, collection: PessoaFisica.all.map {|responsavel| [responsavel.nome, responsavel.id]}, prompt: 'Selecione o responsável' %>
  <%= f.input :cpf %>
  <%= f.input :nome %>
  <%= f.input :status %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                dependentes_path, :class => 'btn btn-default' %>
<% end %>
